name: prerelease

# master への取り込みを契機に、test チャンネル向けのプレリリースを自動作成する (issue #45)。
#
# バージョンはタグ運用から決める。安定版タグ `X.Y.Z` のうち最新のものからパッチを
# 1 つ上げた `X.Y.(Z+1)` を次期バージョンとし、プレリリース番号を付けた
# `X.Y.(Z+1)-testN` をタグにする。同じ次期バージョンの `-testN` が既にあれば N を進める。
# パッチ以外 (マイナー・メジャー) を上げたいプレリリースは手動でリリースを作成する。
#
# 成果物の生成と添付は release.yml に任せず、このワークフロー内で行う。
# GITHUB_TOKEN で作成したリリースは、ワークフローの連鎖起動を防ぐ GitHub Actions の
# 仕様により `release` イベントを発火させないため、release.yml はここで作った
# プレリリースに対しては動かない。
# 安定版リリースは手動作成であり、その場合は `release` イベントが発火して
# release.yml が成果物の添付を行う。

on:
  push:
    branches:
      - master
  # タグ計算からやり直したい場合などに手動でも作成できるようにする
  workflow_dispatch:

permissions:
  contents: read

jobs:
  # 成果物の中身が変わる変更かどうかを判定する。
  #
  # `on` の `paths` フィルタではなくジョブで判定するのは、判定できなかった場合に
  # 作成する側へ倒すため。`paths` フィルタは、比較できない push や件数の上限を超えた
  # 変更をどう扱うかを制御できず、黙って起動しないという最悪の側へ倒れる。
  #
  # 対象パスの一覧はこのジョブだけに置く。`on` 側にも書くと二重管理になり、
  # 片方だけ更新したときに「変更したのにプレリリースが作られない」事故につながる。
  changes:
    name: 変更したパスを見る
    runs-on: ubuntu-latest
    outputs:
      relevant: ${{ steps.check.outputs.relevant }}
    steps:
      - id: check
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          EVENT_NAME: ${{ github.event_name }}
          BEFORE: ${{ github.event.before }}
          AFTER: ${{ github.sha }}
        run: |
          set -euo pipefail

          # workflow_dispatch など、比較対象を定められないイベントでは常に作成する
          if [ "$EVENT_NAME" != "push" ]; then
            echo "relevant=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # ブランチ作成時や force push 後は `before` が到達不能なコミットを指し比較できない
          if [ -z "$BEFORE" ] || [ "$BEFORE" = "0000000000000000000000000000000000000000" ]; then
            echo "relevant=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # 1 ファイルにつき 1 行を出す。件数を数えるためで、行数がそのままファイル数になる。
          # リネームで対象外へ移した変更 (src/A.cs → docs/A.cs など) は新パスだけでは
          # 検出できないため、previous_filename も同じ行へ並べる。
          RENAME_AWARE='[.filename, (.previous_filename // "")] | @tsv'
          # `?` を付けず、想定外のレスポンスでは jq を失敗させる。
          # 黙って空の一覧として扱うと、判定できない変更を作成せずに終えてしまう。
          JQ=".files[] | $RENAME_AWARE"
          # compare の `files` が返すのは最大 300 件
          FILE_LIMIT=300

          # 差分が分からないまま省略すると、試せるはずの変更が master に入ったままになるため、
          # 取得に失敗した場合は作成する側へ倒す。
          if ! CHANGED=$(gh api --paginate "repos/$REPO/compare/$BEFORE...$AFTER" --jq "$JQ"); then
            echo "::warning::変更ファイルの一覧を取得できなかったためプレリリースを作成します"
            echo "relevant=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # ZIP の中身に効くファイルだけを対象とする。
          # ビルドスクリプトは ZIP の構成そのものを決めるため対象に含める。
          #
          # tests とドキュメントは成果物に入らないため対象外。
          # ワークフロー自身も対象外とする。手順を変えても成果物の中身は変わらず、
          # 中身の同じプレリリースが増えるだけになる。
          # 作り直したいときは `workflow_dispatch` から実行する。
          RELEVANT=false
          COUNT=0
          while IFS=$'\t' read -r NEW_PATH OLD_PATH; do
            [ -n "$NEW_PATH" ] || continue
            COUNT=$((COUNT + 1))
            for FILE in "$NEW_PATH" "$OLD_PATH"; do
              [ -n "$FILE" ] || continue
              case "$FILE" in
                src/*|scripts/build-release.ps1|VRCToolsDataSync.slnx)
                  echo "成果物の中身が変わる変更: $FILE"
                  RELEVANT=true
                  ;;
              esac
            done
          done <<< "$CHANGED"

          # `--paginate` はページを辿るだけで、エンドポイントごとの返却件数の上限は外さない。
          # 上限に達した一覧には、返ってこなかった変更が残っている可能性がある。
          # 完全だと確認できない一覧を根拠に見送るわけにはいかないので、作成する側へ倒す。
          if [ "$RELEVANT" = "false" ] && [ "$COUNT" -ge "$FILE_LIMIT" ]; then
            echo "::warning::変更ファイルが上限の${FILE_LIMIT}件に達し一覧が不完全な可能性があるためプレリリースを作成します"
            RELEVANT=true
          fi

          if [ "$RELEVANT" = "false" ]; then
            echo "::notice::成果物の中身が変わっていないためプレリリースを作成しません"
          fi
          echo "relevant=$RELEVANT" >> "$GITHUB_OUTPUT"

  prerelease:
    name: プレリリースを作る (Windows)
    needs: changes
    if: needs.changes.outputs.relevant == 'true'
    runs-on: windows-latest
    permissions:
      # タグとリリースの作成に必要
      contents: write
    # マージが連続した場合にタグの計算が競合しないよう、実行を直列にする。
    # 並行して走ると両方が同じ `-testN` を計算し、片方のタグ作成が失敗する。
    #
    # バージョンの計算とタグの作成を同じジョブに置いているのはこのためである。
    # 別のジョブへ分けると、間に他の実行が割り込んでも直列化が効かない。
    # x64 と arm64 を matrix で並列にせず同じジョブで続けてビルドするのも同じ理由で、
    # ビルドを別ジョブへ出すと計算と作成の間が直列化の外に出る。
    #
    # ワークフロー全体ではなくこのジョブに効かせる。全体に効かせると、成果物の中身を
    # 変えない push もグループを占有し、待機中の「中身が変わる変更」の実行を取り消したうえで
    # 自身は何も作らずに終わってしまう。
    #
    # `cancel-in-progress: false` が守るのは実行中のものだけで、待機できる実行は 1 件に限られる。
    # 後から入った実行は待機中の実行を取り消すため、取り込みが続けて 3 件以上あると、
    # 中間のコミットを起点とする実行は取り消され、そのコミット単体のプレリリースは作られない。
    # 取り消されたコミットの変更もその実行がチェックアウトする履歴に含まれるため、
    # 最新の内容はプレリリースに入る。
    # 目的は取り込みごとに 1 つ作ることではなく、master の最新の内容を試せるようにすることなので、
    # 中間のコミット単体のプレリリースが飛ぶことは許容する。
    concurrency:
      group: prerelease
      cancel-in-progress: false
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # バージョンをタグの一覧から決めるため、履歴とタグをすべて取得する
          fetch-depth: 0

      - name: バージョンを決める
        id: version
        shell: bash
        run: |
          set -euo pipefail

          # 最新の安定版タグ (X.Y.Z 形式) を起点にする。
          # sort -V は環境によって使えないことがあるため、フィールドごとの数値順で並べる。
          LATEST=$(git tag --list \
            | grep -E '^[0-9]+\.[0-9]+\.[0-9]+$' \
            | sort -t. -k1,1n -k2,2n -k3,3n | tail -n 1 || true)

          # 安定版タグがまだ無ければ 0.0.1 を次期バージョンとする。
          # 旧 release.yml の自動採番と同じ初期値である。
          if [ -z "$LATEST" ]; then
            NEXT="0.0.1"
          else
            NEXT="${LATEST%.*}.$(( ${LATEST##*.} + 1 ))"
          fi

          # 既存の `-testN` の最大値から次の N を決める。
          # N がゼロ埋めされたタグもありうるため、先頭の 0 を落としてから数値として比較する。
          MAX_N=$(git tag --list "${NEXT}-test*" \
            | sed -nE 's/^.*-test0*([0-9]+)$/\1/p' | sort -n | tail -n 1)
          N=$(( ${MAX_N:-0} + 1 ))

          VERSION="${NEXT}-test${N}"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "バージョン: $VERSION"

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
          dotnet-quality: 'preview'

      - name: Restore
        run: dotnet restore VRCToolsDataSync.slnx

      - name: Publish (App + Cli, x64)
        shell: pwsh
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: ./scripts/build-release.ps1 -Arch x64 -Version $env:VERSION

      - name: Publish (App + Cli, arm64)
        shell: pwsh
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: ./scripts/build-release.ps1 -Arch arm64 -Version $env:VERSION

      - name: プレリリースを作る
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.version }}
          # 起動時点のコミットへタグを打つ。省略するとタグ作成時のデフォルトブランチの
          # 先頭が使われ、実行が遅延した場合に後続のコミットを巻き込んでしまう
          target_commitish: ${{ github.sha }}
          prerelease: true
          generate_release_notes: true
          files: artifacts/*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
